const h2Element$ = document.querySelector('h2.fn-insert-here');
h2Element$.innerHTML = 'Wubba Lubba dub dub';

//h2Element$.textContent = 'Wubba Lubba dub dub';
//en vez de innerHTML se puede usar textContent

// We can Check if the element is found
//if (h2Element$) {
    // Insert the text 'Wubba Lubba dub dub' into the h2 element
   ////h2Element$.innerHTML = 'Wubba Lubba dub dub';
//} else {
    // Handle case when element is not found
    //console.error('Element with class "fn-insert-here" not found.');
//}
